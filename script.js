// Function to navigate to other pages
function goToPage(page) {
    alert("This will go to " + page + " (page not implemented yet).");
    // Uncomment the line below when pages exist:
    // window.location.href = page;
}