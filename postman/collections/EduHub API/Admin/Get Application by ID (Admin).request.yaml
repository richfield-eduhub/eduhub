$kind: http-request
name: Get Application by ID (Admin)
method: GET
url: "{{base_url}}/admin/applications/:id"
headers:
  Authorization: Bearer {{access_token}}
pathVariables:
  - key: id
    value: "{{application_id}}"
    description: Application UUID
order: 2000
