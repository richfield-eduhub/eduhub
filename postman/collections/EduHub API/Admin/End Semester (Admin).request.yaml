$kind: http-request
name: End Semester (Admin)
method: POST
url: "{{base_url}}/admin/semesters/:id/end"
headers:
  Authorization: Bearer {{access_token}}
pathVariables:
  - key: id
    value: "{{semester_id}}"
    description: Semester UUID to end
order: 1800
