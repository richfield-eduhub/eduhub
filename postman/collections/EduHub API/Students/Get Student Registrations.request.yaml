$kind: http-request
description: Get all module registrations for a student. Owner or staff only.
url: "{{base_url}}/students/:id/registrations"
method: GET
headers:
  Authorization: Bearer {{access_token}}
pathVariables:
  id: student_uuid_here
order: 5000
